package com.akinar;

import java.io.*;

public class DeleteRecord {

    // Здесь необходимо создать метод, который при помощи scanner запросит у пользователя номер
    // строки для удаления.
    // Затем данные без удаляемой строки перепиываются в новый файл, старый файл удаляется, а новый
    // переименовывается.
}
